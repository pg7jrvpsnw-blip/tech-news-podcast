# Python
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# venv
.venv

# secrets
.env

# 不再排除 mp3:由于 mify 是内网,GH Actions 跨不了,改成本地 launchd 跑 + push,
# mp3 进 Git 由 CF Pages 直接服务(单仓库一年约 1GB,可接受)。
# 如果某天换 R2 / git-lfs,再来加排除规则。

# 系统
.DS_Store
